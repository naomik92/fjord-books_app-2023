<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('defaults.edit', item: Book.model_name.human), edit_book_path(@book) %> |
  <%= link_to t('defaults.back_to_list', item: Book.model_name.human(count: 2)), books_path %>
  <%= button_to t('defaults.destroy', item: Book.model_name.human), @book, method: :delete %>
</nav>
