<%= form_with(model: [report, report.comments.build]) do |form| %>
  <div>
    <%#= form.label :name, style: "display: block"  %>
    <%#= form.text_field :name %>
  </div>

  <div>
    <%= form.label :comment, style: "display: block"  %>
    <%= form.text_area :comment %>
  </div>

  <%= form.submit %>
<% end %>
