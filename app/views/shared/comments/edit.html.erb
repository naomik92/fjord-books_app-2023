<h1><%= t('views.common.title_edit', name: Comment.model_name.human.downcase) %></h1>

<%= render "shared/comments/form", commentable: @comment.commentable, comment: @comment %>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(Comment.model_name.human.downcase)), :back %>
</nav>
